# Supabase 대시보드 → Settings → API
SUPABASE_URL=https://xxxx.supabase.co
# API Keys → Secret keys 에서 생성 (서버 전용, 노출 금지)
SUPABASE_SERVICE_ROLE_KEY=sb_secret_...
# 관리자 삭제용 키 (openssl rand -hex 16 등으로 생성)
ADMIN_KEY=...
